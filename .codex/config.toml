# Project-local Codex configuration (safe defaults).
# Put secrets in environment variables, not here.

[project]
name = "sound-sanctuary"

# MCP servers (placeholders). Enable the ones you actually use.
# IMPORTANT: Only run MCP servers you trust.

[mcp_servers]

# Figma MCP (use when you have a Figma file as source of truth)
# figma = { command = "npx", args = ["-y", "figma-mcp-server"], env = { FIGMA_ACCESS_TOKEN = "${FIGMA_ACCESS_TOKEN}" } }

# Playwright MCP (use for QA & regression checks)
# playwright = { command = "npx", args = ["-y", "@playwright/mcp"] }

# Docs MCP (optional, for pulling official docs context)
# docs = { command = "npx", args = ["-y", "docs-mcp"] }
