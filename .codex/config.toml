# Project-local Codex configuration (safe defaults).
# Do NOT commit secrets here. Use environment variables.

[project]
name = "sound-sanctuary"

# MCP servers (optional). Enable only what you actually trust and use.
# The examples below are placeholders.

[mcp_servers]

# --- Figma MCP ---
# Use when you have a Figma file as the design source of truth.
# figma = { command = "npx", args = ["-y", "figma-mcp-server"], env = { FIGMA_ACCESS_TOKEN = "${FIGMA_ACCESS_TOKEN}" } }

# --- Playwright MCP ---
# Use for QA/regression checks; agent can open pages, check layout, etc.
# playwright = { command = "npx", args = ["-y", "@playwright/mcp"] }

# --- Docs MCP ---
# Optional helper to pull official docs context.
# docs = { command = "npx", args = ["-y", "docs-mcp"] }
